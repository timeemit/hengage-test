%h1 Listing projects

%p= link_to 'Make a New Project', new_project_path

%table
	%tr
		%th Name
		%th
		%th

	- @projects.each do |project|
		%tr
			%td= project.name
			%td= link_to 'Show', project
			%td= link_to 'Edit', edit_project_path(project)

%p= link_to 'Make a New Project', new_project_path
